enum Command
{
    COUNT
    LIMIT
    OR_WHERE
    OR_WHERE_HAS
    OFFSET
    ORDER_BY
    WHERE
    WHERE_HAS
    WHERE_IN
    WHERE_JSON_CONTAINS
}

enum Operator 
{
    ASC
    CONTAINS
    DESC
    EQUALS
    GREATER
    GREATER_OR_EQ
    IN
    IS_NOT_NULL
    IS_NULL
    LOWER
    LOWER_OR_EQ
    NOT_CONTAINS
    NOT_EQUALS
}